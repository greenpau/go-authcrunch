interface:
  display_name: "Reusable OIDC Provider"
  short_description: "Maintain OIDC provider, browser pages, and conformance"
  default_prompt: "Use $authentication-portal-oidc to maintain the reusable OpenID Provider, its public API, portal browser rendering, and protocol and conformance tests."
