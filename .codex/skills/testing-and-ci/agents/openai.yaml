interface:
  display_name: "Testing and CI"
  short_description: "AuthCrunch tests, E2E, scoped diagnostics, and CI"
  default_prompt: "Use $testing-and-ci to remediate applicable diagnostics immediately while editing agent-owned code, develop corresponding tests and E2E coverage, and run the right AuthCrunch validation workflow."
